# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Notice-Solver** is a project that crawls bulletin boards (게시판) to aggregate public notices and convert them into a structured Knowledge Base. The README is written in Korean.

## Current State

This repository is in its initial stage — no source code, build system, dependencies, or tests exist yet. The only file is `README.md`.

When implementation begins, update this file with:
- Language/runtime and how to install dependencies
- Build, lint, and test commands (including how to run a single test)
- Architecture overview once the crawling pipeline and KB storage layer take shape
